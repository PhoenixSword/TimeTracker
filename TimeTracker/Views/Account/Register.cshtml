@using TimeTracker.ViewModels

@model RegisterViewModel
<h2>Register</h2>

<div class="row">
    <div class="col-md-4">
        <form asp-route-returnUrl="/" asp-controller="Account" asp-action="Register" method="post">
            <h4>Create a new account.</h4>
            <hr />
            <div asp-validation-summary="All" class="text-danger"></div>
            <div class="form-group">
                <label asp-for="@Model.Email"></label>
                <input asp-for="@Model.Email" class="form-control" />
                <span asp-validation-for="@Model.Email" class="text-danger"></span>
            </div>
            <div class="form-group">
                <label asp-for="@Model.Password"></label>
                <input asp-for="@Model.Password" class="form-control" />
                <span asp-validation-for="@Model.Password" class="text-danger"></span>
            </div>
            <div class="form-group">
                <label asp-for="@Model.ConfirmPassword"></label>
                <input asp-for="@Model.ConfirmPassword" class="form-control" />
                <span asp-validation-for="@Model.ConfirmPassword" class="text-danger"></span>
            </div>
            <button type="submit" class="btn btn-default">Register</button>
            <button type="button" class="btn btn-default" onclick=regWithPassword()>reg in firebase</button>
            <button type="button" class="btn btn-default" onclick=Check()>reg test</button>
        </form>
    </div>
</div>

@section Scripts {
    <partial name="_ValidationScriptsPartial" />
}


<script src="https://www.gstatic.com/firebasejs/5.9.2/firebase.js"></script>
<script>
  // Initialize Firebase
  var config = {
    apiKey: "AIzaSyDqloo9E7FZ_je4f4AgcH4MOTaBpPXRDA8",
    authDomain: "timetracker-31fe5.firebaseapp.com",
    databaseURL: "https://timetracker-31fe5.firebaseio.com",
    projectId: "timetracker-31fe5",
    storageBucket: "timetracker-31fe5.appspot.com",
    messagingSenderId: "595182452663"
  };
  firebase.initializeApp(config);
</script>

<script src="https://www.gstatic.com/firebasejs/4.1.3/firebase.js"></script>
<script>
    function regWithPassword() {
    debugger
        var email = document.getElementsByTagName("input")[0].value;
        var password = document.getElementsByTagName("input")[1].value;
        console.log(email);
        console.log(password);
        firebase.initializeApp(config);
        firebase.auth().createUserWithEmailAndPassword(email, password).then(function (result) {
            var token = result.credential.accessToken;
            var user = result.user;
            alert("login OK");
            console.log(user);
            user.getToken().then(function (t) {
                token = t;
                console.log(token);
                //loginAPI();
            });
        }).catch(function (error) {
            var errorCode = error.code;
            var errorMessage = error.message;
            alert(errorCode + " - " + errorMessage);
        });
    }

     function Check() {
        $.ajax({
            url: "http://localhost:62533/Account/CreateUserTest",
            type: 'POST',
            error: function (ex) {
                console.log(ex.status + " - " + ex.statusText);
            },
            success: function (data) {
                console.log(data);
                return data;
            }
        });
    }
</script>